// // larger number
// // prompt for two numbers
// // display the larger one

// const larger = (a, b) => (a > b ? a : b);
// console.log(larger(24, 34));

// // display if product is a positive or negative number
// const posNeg = (a, b, c) => (a * b * c >= 0 ? 'positive' : 'negative');
// console.log(posNeg(4, 1, 5));
